#import "di.typ": bibliography-item, conf, math-note, mit-red, light-gray

#show: conf.with(
  title: "剥离作者的阴影: Bamman 文学角色计算模型",
  authors: ("Digital Inhumanities 数字不人文研究中心",),
  abstract: [在大规模数字人文研究中，如何将“作者的个人文风”与“角色的真实人设”彻底剥离？本文将通过一场假想的“编辑部对齐实验”，带你像剥洋葱一样拆解 David Bamman 的贝叶斯混合效应模型。我们将把复杂的算法（Dirichlet, SAGE, Gibbs, Laplace）化作破案的工具，娓娓道来这场名为“语义脱水”的数学推演。],
  keywords: ("数字人文", "SAGE模型", "Dirichlet分布", "稀疏性"),
)

= 1. 陷入死胡同的“直觉统计”

在文学人物聚类的早期探索中，学者们抱着一种朴素的#strong[直觉观点]：只要把一个角色周围经常出现的词汇全抓出来，统计一下频率，就能勾勒出这个角色的“人设”了。

然而，当这种方法被扔进几万本跨越数百年的小说堆里时，遭遇了惨烈的滑铁卢：
- #strong[偏差被无限放大]：不同作者描写同一个类型（比如“反派”）的用词天差地别。
- #strong[高维空间里的迷路]：当这些带有强烈作者偏好的词汇频率被转换成高维语义向量后，K-means 等聚类算法会直接被带偏。
- #strong[一团乱麻的轮廓系数]：聚出来的结果，簇与簇之间像浆糊一样分不开，聚类中心也是一堆毫无性格的常用词。最终，还是得靠语言学家回去翻原著，“凭直觉”去强行解释。

问题出在哪？出在那个在打字机前敲键盘的“幽灵”——#strong[作者]。Bamman 敏锐地意识到：作者的写作风格就像一个强大的滤镜，它是一个#strong[隐藏的概率分布]，一直在默默干扰我们对角色用词的观察。

= 2. 当奥斯汀与狄更斯来到 #strong[数字不人文研究中心]

为了看清这个“滤镜”有多可怕，我们不妨设定一个极其微观的案发现场。

我们要让两位文风迥异的带文豪——#strong[简·奥斯汀]与#strong[查尔斯·狄更斯]，分别去描写我们公众号的两位固定人设：
- #strong[人设 A (羊咩咩主编)]：核心标签是“治愈、学术、亲和”。
- #strong[人设 B (鱼摆摆副主编)]：核心标签是“极客、思考、硬核”。

如果我们让算法直接去原著里“数词儿”，我们会得到两份由于作者滤镜不同，而产生严重“语义错位”的口供：

#v(0.5em)
#align(center)[
  *表 1：奥斯汀笔下的描述 (Raw)*
  #table(
    columns: (1fr, 1.5fr, 4fr),
    stroke: 0.5pt + mit-red,
    fill: (x, y) => if y == 0 { mit-red.lighten(95%) },
    [*角色*], [*依存关系 (Role)*], [*观测词汇与概率*],
    table.cell(rowspan: 4, align: center + horizon)[羊咩咩], [动作 (Agent)], [安慰 (0.15), 倾听 (0.08), 微笑 (0.11)],
    [受事 (Patient)], [被称赞 (0.12), 被感谢 (0.09)],
    [拥有 (Possessive)], [举止 (0.18), 书信 (0.06), 情感 (0.05)],
    [描述 (Predicative)], [温柔的 (0.14), 聪慧的 (0.10)],
    table.cell(rowspan: 4, align: center + horizon)[鱼摆摆], [动作 (Agent)], [观察 (0.16), 思考 (0.11), 辩论 (0.08)],
    [受事 (Patient)], [被误解 (0.11), 被反驳 (0.07)],
    [拥有 (Possessive)], [品味 (0.13), 见识 (0.09), 财产 (0.05)],
    [描述 (Predicative)], [骄傲的 (0.15), 理智的 (0.12)],
  )
]

#v(1em)

#align(center)[
  *表 2：狄更斯笔下的描述 (Raw)*
  #table(
    columns: (1fr, 1.5fr, 4fr),
    stroke: 0.5pt + mit-red,
    fill: (x, y) => if y == 0 { mit-red.lighten(95%) },
    [*角色*], [*依存关系 (Role)*], [*观测词汇与概率*],
    table.cell(rowspan: 4, align: center + horizon)[羊咩咩], [动作 (Agent)], [哭泣 (0.14), 祈祷 (0.09), 忙碌 (0.07)],
    [受事 (Patient)], [被遗弃 (0.16), 被拯救 (0.11)],
    [拥有 (Possessive)], [起居室 (0.17), 眼泪 (0.12), 善良 (0.08)],
    [描述 (Predicative)], [可怜的 (0.15), 天真的 (0.10)],
    table.cell(rowspan: 4, align: center + horizon)[鱼摆摆], [动作 (Agent)], [算计 (0.15), 咆哮 (0.08), 凝视 (0.07)],
    [受事 (Patient)], [被畏惧 (0.12), 被起诉 (0.09)],
    [拥有 (Possessive)], [肮脏的 (0.18), 法律 (0.13), 账本 (0.06)],
    [描述 (Predicative)], [阴郁的 (0.13), 固执的 (0.11)],
  )
]
#v(0.5em)

你看，奥斯汀书里的“羊咩咩”总是有着良好的“举止”，而狄更斯笔下的“羊咩咩”则总是待在“起居室”里。#strong[算法的终极目标，就是从这些凌乱的表象中，识别出奥斯汀的“举止”和狄更斯的“起居室”，背后其实共享了同一个属于“羊咩咩（治愈型）”的数学分布。]

= 3. 巨人的肩膀：用加法拟合世界 (Additive)

既然直接找人设找不到，我们不如换个思路。Bamman 提出了一个绝妙的假设：当我们选择了一个角色 $c$，和她的人物抽象类型 $p$（比如羊咩咩），再让作者 $a$（比如奥斯汀）去描写她时，产生的那个词语 $w$（比如“书信”），其实是由几个独立因素#strong[共同叠加]造成的。

这就是 SAGE 模型中最核心的 #strong[Additive（加法）] 属性。在对数空间里，我们将这种影响的深度拆解成了三个独立参数向量（$eta$）的连加：

$
  P(w | m, p, r, eta) = exp(eta^0_(r, w) + eta^"meta"_(r, w, m) + eta^"pers"_(r, w, p)) / ( sum_(v=1)^V exp(eta^0_(r, v) + eta^"meta"_(r, v, m) + eta^"pers"_(r, v, p)) )
$

#math-note(title: "数学小贴士：条件概率与 Logit")[
  这里的 $P(w | dots)$ 计算的是在已知作者 $m$、人设 $p$ 和依存关系 $r$ 的条件下，观测到词语 $w$ 的#strong[条件概率]。而在词汇树的二元路径选择中，模型使用了 $"logit"^(-1)$ 函数，即 $sigma(x) = 1 / (1 + exp(-x))$。它将实数域的得分（Logits）映射到 $(0, 1)$ 区间，从而将其解释为概率。
]

- $eta^0_w$（#strong[时代底噪]）：十九世纪大家都爱说话（“talk”）。
- $eta^"meta"_(w, m)$（#strong[作者的偏离]）：奥斯汀偏偏非常喜欢写“书信”这个词。
- $eta^"pers"_(w, p)$（#strong[人设的偏离]）：羊咩咩作为治愈学术型人设，本身也常常与“书信”产生正向联系。

#strong[让我们代入刚才的案发现场算一笔账：]
如果时代底噪对“书信”的打分是 $-1.5$，奥斯汀的作者偏好给它加了 $2.8$ 分，羊咩咩的人设偏好给它加了 $1.2$ 分。那么最终在奥斯汀书里，羊咩咩带出“书信”的总得分就是 $-1.5 + 2.8 + 1.2 = 2.5$。

通过这种对数线性的加法机制，模型在数学空间里实现了变量的解耦，从而得以把 $2.8$ 的“奥斯汀滤镜”精准剥离，留下 $1.2$ 的纯粹人设。

=== 算力危机与拯救：构建“词汇树” (Cluster Tree)

你看上面那个公式的分母，它需要把词库里所有的词（$V$）的得分都算一遍来求和。当我们的 $eta$ 参数维度极大、真实世界的词汇量也极大时，每次都要全局求和会让计算机算力彻底崩溃。

为了优化计算，模型在这里引入了#strong[聚类树（Cluster Tree）]的方法。
具体来说，每个角色都有 4 棵很大的树，对应 4 种依存关系（动作、受事、拥有、描述）。这 4 种关系下产生的所有具体词语，就是这棵树上的#strong[叶子]。通过这种树状结构，我们可以将原本对十万个词汇的扁平化 Softmax 计算，转化为一连串的二元逻辑回归（Hierarchical Softmax）。

为了计算一个词的概率，模型实际上是在计算从树根走到该词对应叶子节点的路径概率。假设一个词被编码为一串二进制位比特串 $b = b_1 b_2 dots b_n$（例如 `01011`），那么选择该词的概率就是路径上每一次选择左或右的概率之积。每一次选择的概率分布如下：

$
  P(b | m, p, r, eta) = product_(j=0)^(n-1) cases(
    "logit"^(-1)(eta^"meta"_(r, b_(1:j))[m] + eta^"pers"_(r, b_(1:j))[p] + eta^0_(r, b_(1:j))) & "if" b_(j+1) = 1,
    1 - "logit"^(-1)(eta^"meta"_(r, b_(1:j))[m] + eta^"pers"_(r, b_(1:j))[p] + eta^0_(r, b_(1:j))) & "otherwise"
  )
$

#strong[公式下标解读：]
- $b$：代表词汇在聚类树中的#strong[二进制比特串编码]（Bitstring representation）。例如 `01011` 对应“书信”。
- $b_(1:j)$：代表从树根开始的前 $j$ 次路径选择（即#strong[决策节点前缀]）。每一次路径分叉，都是在做一次微型的 SAGE 加法效应计算。
- $r$：代表依存关系（如 Possessive）。
- $m$ 和 $p$：依旧代表作者和人设。

于是，模型真正要建立的，是一张极其复杂的#strong[地图查询表]：
当我知道我在哪棵树上（哪种依存关系 $r$）、在哪个作家的笔下（作者 $m$）、以及面对什么人物类型（人设 $p$）时，模型就会告诉我：在这个路口节点 $b_(1:j)$，我是该选择去树杈的左边（对应概率 $1 - "logit"^(-1)$），还是去树杈的右边（对应概率 $"logit"^(-1)$）？

这样，极其昂贵的“全局词汇概率求和”，就被优化成了高效的“顺藤摸瓜”式查找，大大减少了参数量，极大地加速了模型的推断过程！

= 4. 导演入场：不能让外星人乱入 (Dirichlet)

加法模型看起来很棒，但如果我们把视野从编辑部拉大到整个语料库——所有的书，所有的词，所有的角色。如果不加任何限制地去用加法拟合，模型很容易“发疯”。

比如，它可能会为了凑概率，硬生生在列夫·托尔斯泰的《战争与和平》里，拟合出几个属于阿西莫夫宇宙的科幻外星人（极客型人设）。这显然是不符合文学常理的。也就是：#strong[我们不希望某一本书里的 Persona（人设）处于一种没有规律的交叉混杂状态。]

为了防止这种失控，模型在生成过程的顶层，请来了一位名叫 #strong[Dirichlet 分布] 的宏观导演。

#math-note(title: "数学小贴士：Dirichlet 分布")[
  在生成模型中，每部小说 $d$ 都会从一个先验 $alpha$ 中抽取一个分布 $theta_d tilde "Dir"(alpha)$。这决定了这本书里的角色配比。\
  Dirichlet 会输出一串概率密度，$alpha$ 越小，分布越集中。
  - 设置 $alpha=0.3$：这本书 80% 都是“羊咩咩型（治愈）”，只有各 10% 是极客或反派。（很符合奥斯汀小说的基调）
  - 设置 $alpha=0.8$：角色分布更均匀， 30% 治愈，30% 极客，40% 反派。
]

有了这位导演，即使奥斯汀笔下的“羊咩咩”和“鱼摆摆”偶尔用了相似的词，模型在分配人设时，也会被“奥斯汀的书里治愈型角色应该更多”这个#strong[先验约束]稳稳拉住。

= 5. 左右互搏：逼近真相的推断 (Stochastic EM)

现在，框架搭好了，约束有了。但真正的难题来了：在案发现场，我们#strong[只知道]奥斯汀用了“书信”，狄更斯用了“账本”（这是观测到的真实词频）。我们#strong[既不知道]角色到底属于什么类型 $p$（这还是个离散变量），#strong[也不知道]具体的时代底噪、作者偏好和人设偏好（也就是那堆 $eta$ 系数）。

这该怎么学？模型陷入了“鸡生蛋，蛋生鸡”的困境：如果不确定羊咩咩的人设，怎么算奥斯汀的作者偏离？如果不知道奥斯汀的作者偏离，又怎么判断她写的角色到底是不是羊咩咩？

为了打破这个死局，Bamman 采用了一种名为 #strong[Stochastic EM（随机期望最大化）] 的策略，它包含两个不断交替的“左右互搏”步骤：#strong[折叠吉布斯抽样（E-step）]和#strong[参数最大化（M-step）]。

=== 步骤一：E-step（吉布斯抽样——给角色贴标签）
在这个阶段，我们#strong[假设 $eta$ 参数是固定的]（哪怕一开始是瞎猜的），任务是为每个角色重新分配一个人设（比如判断她是不是“羊咩咩”型，记为人设 $z$）。
吉布斯抽样会计算这样一个条件概率：

$
  P(p_c = z | dots) prop underbrace((N_(d, -c)^((z)) + alpha_z), "Dirichlet 先验平滑") times underbrace(product_(j: r_j=r) P(w_j | m, z, r, eta), "当前选词的似然概率")
$

#strong[像侦探一样思考公式：]
1. #strong[看大环境（先验）]：这本书里，除了当前角色外，被归类为“治愈型（羊咩咩）”的角色多不多？如果这本来就是一本奥斯汀的治愈小说，Dirichlet 平滑项会给出很高的基础分。
2. #strong[看具体口供（似然）]：既然我们已经假设了奥斯汀的 $eta^"meta"$（比如知道她爱用“举止”），我们把该角色周围的“举止”、“安慰”这些词代入第 3 节的 SAGE 加法公式。如果在减去奥斯汀的偏好后，剩下的得分完美符合“羊咩咩”的人设 $eta^"pers"$，这部分的似然概率就会暴涨。

结合这两点，模型像掷一枚修改了重心位置的骰子一样，为这个角色重新抽取了一个离散的人设标签 $p$。因为重心变化逐渐稳定，抽样的结果也会越来越稳定。

=== 步骤二：M-step（参数最大化——调整滤镜系数）
所有角色的标签都贴好后，模型反过来：#strong[假设角色的人设 $p$ 全是正确的]，现在我们要去调整所有的 $eta$ 系数，让目前观测到的“书信”和“账本”的出现概率（总似然）达到最大。

#strong[像对质口供一样调整：]
如果模型发现在被贴上“极客型（鱼摆摆）”标签的角色身上，狄更斯大量使用了“肮脏的”和“账本”；而奥斯汀描写同样的“鱼摆摆”时用了“辩论”和“理智”。
在最大化对数似然的过程中，算法为了“最合理地解释这种词汇差异”，会被迫调整权重：把“肮脏的”高分分配给狄更斯的 $eta^"meta"$（作者滤镜），而把“理智的”、“思考”的高分分配给极客型的 $eta^"pers"$（核心人设）。

#strong[循环往复]：模型就这样在 E-step 重新贴标签，在 M-step 重新调滤镜。几千次迭代后，生成的概率越来越逼近真实的统计频率，最终收敛到一个让整套逻辑最自洽的“真相”。

#math-note(title: "数学小贴士：M-step 的优化目标")[
  在 M-step 中，模型的目标是找到一组最优参数 $\hat{eta}$，使得观测数据的对数似然最大化，同时满足稀疏性约束。其优化函数可表示为：
  $ \hat{eta} = arg max_(eta) sum_(j) log P(w_j | m, p, r, eta) - lambda ||eta||_1 $
  这里 $lambda ||eta||_1$ 是 $L_1$ 正则项（由拉普拉斯先验引入）。优化过程采用 #strong[OWL-QN 算法]。
  
  #v(0.5em)
  #set text(size: 8pt)
  #strong[名词解释：拟牛顿法 (Quasi-Newton)]\
  这是一种寻找函数极值的高级算法。由于直接计算二阶导数（牛顿法）在海量参数下太慢，拟牛顿法通过“模拟”二阶导数的信息，既保留了快速收敛的优点，又大大节省了计算量。OWL-QN 则是其专门针对 $L_1$ 正则化（不可导点）的变体。
]

= 6. 严厉的裁判：逼出纯粹的残差 (Sparse)

在这场几千次的左右互搏中，还有一个致命隐患：如果模型为了偷懒，把一丁点细微的频率变化，都算作是“奥斯汀的偏离”或者“羊咩咩的偏离”，慢慢累积起来，人设的词簇就会变得无比臃肿。

我们不希望偏离是每个因素都造成一点影响形成的累积，我们想要的是——#strong[某一些特殊因素引起的强烈偏离]！

所以在优化 $eta$ 参数（M-step）时，模型引入了一个极其严厉的裁判：#strong[拉普拉斯先验（对应 $L_1$ 正则化）]：
$ eta^"meta", eta^"pers" ~ "Laplace"(0, lambda) $

这个裁判强迫绝大多数没有明显证据的“偏离系数”归零。只有那些真正定义了 Persona 的词，才能获得非零的 $eta^"pers"$。这种强迫，在 SAGE 模型中被称为 #strong[Sparse（稀疏性约束）]。它确保了定义“羊咩咩”的词簇是有限而精确的。

#math-note(title: "数学小贴士：拉普拉斯先验与 L1 稀疏性")[
  #strong[L1 范数] 是指向量中各个元素#strong[绝对值之和]：$||eta||_1 = sum |eta_i|$。

  #strong[拉普拉斯分布] 的概率密度函数为 $f(x) = 1 / (2b) exp(- (|x-mu|) / b)$。在 SAGE 模型中，我们设定 $\mu=0$。
  
  #strong[先验概率 (Prior Probability)] 是指我们在观测到具体文本数据之前，对参数分布的一种“成见”或假设。拉普拉斯先验就是假设绝大多数参数都应该是 0。

  #let plot-laplace() = {
    let w = 120pt
    let h = 60pt
    let steps = 100
    let points = ()
    for i in range(0, steps + 1) {
      let x = (i / steps - 0.5) * 4 
      let y = calc.exp(-calc.abs(x)) * 0.5
      points.push((i / steps * w, (1 - y) * h))
    }

    align(center)[
      #box(stroke: 0.5pt + rgb("#555555"), inset: 8pt, radius: 2pt)[
        #stack(dir: ltr, spacing: 1em,
          stack(dir: ttb, spacing: 0.5em,
            text(size: 8pt, fill: mit-red, weight: "bold")[拉普拉斯分布 (L1)],
            box(width: w, height: h)[
              #place(bottom, line(length: w, stroke: 0.5pt + rgb("#555555")))
              #place(left + bottom, line(start: (w/2, 0pt), end: (w/2, -h), stroke: 0.5pt + rgb("#555555")))
              #place(top, curve(stroke: 1.5pt + mit-red, curve.move(points.at(0)), ..points.slice(1).map(p => curve.line(p))))
            ]
          ),
          align(left + horizon)[
            #set text(size: 8pt)
            - #strong[输入]：权重系数 $eta$
            - #strong[输出]：该系数的先验概率
            - #strong[特性]：0 点处有#strong[极尖的峰]。
            - #strong[后果]：产生向心力，把权重归零。
          ]
        )
      ]
    ]
  }

  #plot-laplace()

  与之相对，高斯分布在 0 点处是平滑的，只会让参数变小而不会使其归零。
]

= 7. 终章：我们如何得到角色的“身份证”？

经过数千轮的“左右互搏”，模型最终会为每个角色 $c$ 计算出一个#strong[后验概率分布] $P(p_c | dots)$。

#v(0.5em)
#set text(size: 9.5pt)
#strong[什么是后验概率 (Posterior)？]\
简单来说，这是我们在“看完书”之后得到的结论。它的计算遵循贝叶斯法则的核心：#strong[后验 $prop$ 先验 $times$ 似然]
。对于一个角色，她属于某种人设 $z$ 的概率计算如下：
$ P(p_c = z | dots) prop underbrace((N_(d, -c)^((z)) + alpha_z), "先验：这本书里其他人设的分布") times underbrace(product_(j) P(w_j | m, z, r, eta), "似然：该角色用词与该人设的匹配度") $

这个公式就是角色的“身份证采集过程”。模型会扫描角色周围所有的依存关系词汇，减去作者滤镜后，看剩下的“残差”最符合哪个人设的特征。

#v(0.5em)
#strong[什么是 MAP (最大后验估计)？]\
MAP 的全称是 #emph[Maximum A Posteriori]。在得到概率清单后（如：羊咩咩 85%，鱼摆摆 10%），MAP 引导我们选择那个概率最大的选项作为该角色的标签。在这个例子中，MAP 验证的是：#strong[在排除了所有干扰因素后，逻辑上最自洽的角色归类是什么。]

#math-note(title: "数学小贴士：JS 散度 (Jensen-Shannon Divergence)")[
  当我们拿到了两个角色的后验分布 $P$ 和 $Q$，如何量化她们的相似度？
  
  #strong[JS 散度] 是 KL 散度的一种对称化变体，公式为：
  $ "JS"(P || Q) = 1 / 2 "KL"(P || M) + 1 / 2 "KL"(Q || M) $
  其中 $M = 1 / 2 (P + Q)$ 是两个分布的均值。
  
  #v(0.5em)
  #set text(size: 8pt)
  - #strong[对称性]：$"JS"(P || Q) = "JS"(Q || P)$，这意味着“A 像 B”等同于“B 像 A”。
  - #strong[有界性]：其值域在 $[0, 1]$ 之间。0 代表分布完全一致，1 代表完全不重叠。
  - #strong[文学应用]：在 Bamman 的模型中，JS 散度越小，意味着两个角色在剔除作者文风后，其核心“人设残差”越接近，她们在文学功能上越属于同类。
]


  #math-note(title: "数学小贴士：SAGE vs. LDA")[
  #strong[相同点]：两者都是生成式概率模型，都利用了 Dirichlet 分布来建模文档层面的隐变量（主题/人设）配比，并常通过 Gibbs 采样进行推断。

  #strong[不同点]：
  - #strong[生成机制]：LDA 假设词汇直接由多项分布产生；SAGE 则是对数线性模型，认为词汇概率是多个效应（背景、作者、人设）的叠加。
  - #strong[核心关注]：LDA 倾向于抓取“共性”的主题词簇；SAGE 关注的是“偏离”，即相对于背景分布而言，特定人设特有的词汇。
  - #strong[稀疏性]：SAGE 通过 $L_1$ 正则化强制产生稀疏解，这让它在处理高维元数据（如成千上万个作者）时比 LDA 更加稳健且具有可解释性。
  ]


